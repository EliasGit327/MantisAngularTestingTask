// ClassName is most important here, all others parameters for presenting theme.
export interface ITheme {
  backgroundColor: string;
  buttonColor: string;
  headingColor: string;
  label: string;
  className: string;
}
